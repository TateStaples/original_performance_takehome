# Agent wiki index

- [[p3f-hash-10op-question]] — 2026-07-28, research, partial. Does a 10-op round body exist? Not found; the fold-in `^nv` and the `^C1` are proved irremovable inside the conjugation/basis-transform family by an exact XOR<->ADD dichotomy lemma, and the `//`/`%`/`cdiv` vocabulary gap is closed negative at depth 1 and 2. Part 2 adds: shipped 11 ops proved optimal UNDER an explicit stage-respecting hypothesis; unconditional bound is only N>=2; three invariant families formally barred; one CORRECTION (0x355ACF09 is in node-value range, O1 survives on a build-time argument). kf>=3 MITM is the only sound remaining route.
- [[p3d-l4-final-round-service]] — 2026-07-28, change, awaiting-review. New default-OFF `dev.py` flag `b3l_safe_leaf_fallback` unblocks final-round L4 service beyond 5 groups (and fixes a latent miscompile it exposed); the P3-D "move 26 L4 group-rounds from round 4 to round 15" claim measures NEGATIVE — best round-15-heavy engine floor 1015 vs shipped realized 1006.
