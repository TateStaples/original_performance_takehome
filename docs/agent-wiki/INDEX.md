# Agent wiki index

- [[p7-t2-partial-lazy-position]] — 2026-08-02, change, awaiting-review. Milestone 1 of the C1* stack built behind default-OFF `dev.py` flag `lazy_position_exit` (T2-partial: elide the position accumulator's upkeep on ring-covered group-epochs, rebuild by Horner at the gather exit). Gates green (perf_takehome 1006, flag-OFF bit-exact on 3 configs, 10/10 value-trace seeds ON) and the mechanism MEASURES NEGATIVE: 1006 -> 1008/1011, census delta -2 valu vec-ops of 5,966. Horner-at-exit costs exactly the folds it deletes, so T2 only pays where the accumulator is never read = L4 served at round 15 = exactly what G-38 refuted; 63 exits, not P3-A's 35. T1 already implemented at setup (tables are built by a scalar subtract + broadcast); its 8-op residue needs 64 words against 3 free. Ring re-mine on the new stream finds 0 new rings, closing P3-E §1.4. Detail: research/strains/p7/STATE.md.
- [[p5h-stoke-superoptimizer]] — 2026-08-02, research, partial. STOKE-style MCMC superoptimizer (`stoke.rs`): arbitrary-constant + fan-out coverage MITM never had; planted-control calibrated (12->10 re-fusion PASS in 150s). ALL campaigns negative: sandwich9 20.9B proposals (existence-blind plateau — negative is WEAK, planted control proves from-scratch recovery doesn't happen at this compute; z3 also timed out), myhash 11->10 (cold chains held the real form, no correct 10-op neighbor), round body, 2-round token slice. Odd-multiplier lemma + analytic back-half inversion (midpoint cost) + bitflip polish are reusable for any future s9-class search. Slice ledger: research/strains/p5h/STATE.md.
- [[p5d-fanout-suffix-search]] — 2026-08-01, research, partial. The fan-out-suffix region P5-B named: exact shape census (1.1e12 wiring shapes at n=10 — enumeration impossible; join-at-4 is the entire enumerable increment and triples P5-B's shape coverage), new validated `fanout_mitm.rs` (DAG+join g(m,r) tabs, planted controls incl. a negative that P5-B's family provably cannot see), two lemmas (additive shifts never bijective => soundly absent from chain suffixes; add-joins absorb into madd addend slots), CEGIS closures: 2-round boundary span 7->5 UNSAT at full constant freedom (the <=19 composite has no local entry point left); hash 11->9 2-deletion family mostly UNSAT, madd-heavy templates TIMEOUT=open. Fleet protocol for g-tier slices in research/strains/p5d/STATE.md.

- [[p3f-hash-10op-question]] — 2026-07-28, research, partial. Does a 10-op round body exist? Not found; the fold-in `^nv` and the `^C1` are proved irremovable inside the conjugation/basis-transform family by an exact XOR<->ADD dichotomy lemma, and the `//`/`%`/`cdiv` vocabulary gap is closed negative at depth 1 and 2. Part 2 adds: shipped 11 ops proved optimal UNDER an explicit stage-respecting hypothesis; unconditional bound is only N>=2; three invariant families formally barred; one CORRECTION (0x355ACF09 is in node-value range, O1 survives on a build-time argument). kf>=3 MITM is the only sound remaining route.
- [[p5b-kf3-global-mitm]] — 2026-08-01, research, partial. The funded kf>=3 global hash MITM: new `global_mitm.rs` (full-shape kf=3 prefixes incl. the parallel shape no prior search had, chain-6 suffixes, sharded slices, planted-control-validated). CLOSED NEGATIVE: full_hash 11->10/->9 (59M 3-prefixes x 6-op chains, core pool), the 2-input 12-op round body at <=10 (184.5M prefixes — first no-fold-in-first-assumption search), both 4-op cross-round spans at depth 3. kf4chained calibrated: ~29B prefixes, 4096-slice resume protocol in research/strains/p5b/STATE.md. 2-round composite at <=19-21 ops is structurally beyond enumeration — needs a different tool class.
- [[p3d-l4-final-round-service]] — 2026-07-28, change, awaiting-review. New default-OFF `dev.py` flag `b3l_safe_leaf_fallback` unblocks final-round L4 service beyond 5 groups (and fixes a latent miscompile it exposed); the P3-D "move 26 L4 group-rounds from round 4 to round 15" claim measures NEGATIVE — best round-15-heavy engine floor 1015 vs shipped realized 1006.
- [[p7-polish-stack]] — P7 polish: shipped-config re-tune and emission re-mine both measure ZERO; with-idx tail lands (+22) but is structurally incompatible with parity_ring. perf_takehome.py unchanged at 1006.
